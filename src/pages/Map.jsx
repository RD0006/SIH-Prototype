import PagePlaceholder from "../components/common/PagePlaceholder";

export default function BorderMap() {
  return (
    <PagePlaceholder
      title="Border Map"
      description="Cameras, zones, targets and operational activity."
    />
  );
}